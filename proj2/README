*** nodejs + mysql dans un même conteneur

-fichier SQL pour mysql
-1 dockerfile avec tout dedans
    -on part de 


> mongoimport --host <host_name> --username <user_name> --password <password> --db <database_name> --collection <collection_name> --file <input_file>

> mongoimport --host localhost --username root --password sebastien --db db --collection articles --file articles.json
----------------------------------------------

-build une image : docker build -t my_img .

-lancer le conteneur : docker run -d --name my_c -p 8080:3000 my_img

-aller sur la page http://localhost:8080/