Option :
    -d : arrière plan
    -p : exposer port
    -t : TTY
    -i : intéractif
    -v : volume


-build une image
    docker build -t my_img .

-lancer le conteneur
    docker run -d --name my_c -p 8080:3000 my_img

-lancer un conteneur avec volume (data-test monté dans /data)
    docker run -ti --name my_c -v data-test:/data my_img

-aller sur la page
    http://localhost:8080/